server:
  port: 8888
spring:
  cloud:
    config:
      server:
        git:
          uri: file://${user.dir}/../config-server-config
          clone-on-start: true
          search-paths: config
management:
  endpoints:
    web:
      exposure:
        include: "*"
